// This file remains for compatibility but the actual entry used for render
// is generated dynamically by `render.js` in a temp location. We still export
// the Video component so tools can resolve it.
export { Video } from "./Video";
